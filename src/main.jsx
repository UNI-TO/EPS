import React from 'react';
import ReactDOM from 'react-dom/client';
import App from './App.jsx';
//TODO  :ci sono anche i file .r, rData , immagini da analizzare , piu il riassunto e file word che puoi usarli per generare altri nuovi .
ReactDOM.createRoot(document.getElementById('root')).render(
    <React.StrictMode>
        <App />
    </React.StrictMode>
);
